import { escapeHtml, formatDate, formatLongDate, formatShortDate, getGreeting, relativeDue, daysFromToday } from './utils.js';
import { deriveStats, filterTasks } from './filters.js';

function priorityBadge(priority){ return `<span class="badge ${priority}"><span class="dot" style="width:4px;height:4px;box-shadow:none"></span>${priority}</span>`; }
function taskCard(t,{showActions=true,completedList=false}={}){
  return `<article class="task-card ${t.completed?'done':''}" data-task-id="${t.id}"><button class="task-check ${t.completed?'checked':''}" data-action="toggle-task" data-id="${t.id}" aria-label="${t.completed?'Reopen':'Complete'} task" aria-pressed="${t.completed}"><span class="material-symbols-outlined" style="font-size:15px">check</span></button><div class="task-content"><h3 class="task-title ${t.completed?'strike':''}">${escapeHtml(t.title)}</h3><p class="task-desc">${escapeHtml(t.description||'No description')}</p><div class="task-meta">${priorityBadge(t.priority)}<span class="badge category">${escapeHtml(t.category)}</span>${t.dueDate?`<span><span class="material-symbols-outlined" style="font-size:13px;vertical-align:-2px">event</span> ${escapeHtml(relativeDue(t.dueDate))}${t.dueTime?` · ${escapeHtml(t.dueTime)}`:''}</span>`:''}</div></div>${showActions?`<div class="task-actions"><button class="icon-btn" data-action="view-task" data-id="${t.id}" aria-label="View task"><span class="material-symbols-outlined">visibility</span></button><button class="icon-btn" data-action="edit-task" data-id="${t.id}" aria-label="Edit task"><span class="material-symbols-outlined">edit</span></button><button class="icon-btn" data-action="delete-task" data-id="${t.id}" aria-label="Delete task"><span class="material-symbols-outlined">delete</span></button></div>`:''}</article>`;
}
function emptyState(title,text,icon='inbox'){return `<div class="empty"><span class="material-symbols-outlined">${icon}</span><h3>${title}</h3><p>${text}</p></div>`;}
function quickAdd(){ return `<form class="quick-add" id="quick-add-form"><div class="quick-prefix"><span class="material-symbols-outlined">task_alt</span></div><input id="quick-add-input" maxlength="140" placeholder="What do you need to get done? Press Enter to add a task..." aria-label="Quick add task title"><button class="btn primary" type="submit">Add <span class="kbd" style="position:static;background:rgba(6,14,32,.3);color:white">↵</span></button></form>`; }

export function renderDashboard(tasks){
  const s=deriveStats(tasks); const today=s.todayTasks; const activeToday=today.filter(t=>!t.completed); const focus=activeToday.slice(0,3); const recentDone=today.filter(t=>t.completed).slice(0,3); const completion=today.length?Math.round(today.filter(t=>t.completed).length/today.length*100):0;
  return `<div class="page"><section class="hero-grid"><div class="glass-card dashboard-hero"><div class="eyebrow"><span class="dot"></span>Autonomous runner active <span class="subtle">/</span> workspace #01</div><h1 class="hero-title">${getGreeting()}, Alex 👋</h1><p class="hero-sub">Here's your productivity overview for today.</p><div style="display:flex;justify-content:space-between;gap:10px;flex-wrap:wrap;margin-top:16px"><div class="card" style="padding:9px 12px;background:rgba(6,14,32,.26);box-shadow:none"><div class="mono" style="font-size:11px">${formatLongDate()}</div><div class="muted" style="font-size:9px">Focus date</div></div><div class="card" style="padding:9px 12px;background:rgba(6,14,32,.26);box-shadow:none"><div class="mono" style="font-size:11px">${s.active} active tasks</div><div class="muted" style="font-size:9px">Workspace queue</div></div></div></div><div class="glass-card trajectory"><div class="trajectory-head"><div><div class="trajectory-title">Daily Trajectory: ${completion}% Complete <span class="badge ${completion>=60?'success':'medium'}" style="text-transform:none;letter-spacing:0">${completion>=70?'On Pace':completion?'In Motion':'No tasks yet'}</span></div><div class="muted" style="font-size:11px">${today.filter(t=>t.completed).length} of ${today.length||0} tasks finished today</div></div><div style="text-align:right"><div class="subtle mono" style="font-size:9px">ACTIVE QUEUE</div><strong class="mono">${activeToday.length}</strong></div></div><div class="trajectory-bar"><span style="width:${completion}%"></span></div><div class="trajectory-scale"><span>Start</span><span>Midday</span><span>Current (${completion}%)</span><span>Wrap-up</span></div></div></section><section class="metric-row"><div class="card metric"><div class="metric-top"><span>Total tasks</span><span class="metric-icon"><span class="material-symbols-outlined">layers</span></span></div><div class="metric-value">${s.total}</div><div class="metric-foot"><span>${s.active} active</span><span class="success">${s.completed} done</span></div></div><div class="card metric"><div class="metric-top"><span>Completed today</span><span class="metric-icon" style="color:var(--success)"><span class="material-symbols-outlined">task_alt</span></span></div><div class="metric-value success">${s.completedToday}</div><div class="metric-foot"><span>${completion}% today</span><span class="mono">${today.length} scheduled</span></div></div><div class="card metric"><div class="metric-top"><span>Remaining today</span><span class="metric-icon" style="color:var(--warning)"><span class="material-symbols-outlined">schedule</span></span></div><div class="metric-value warning">${Math.max(0,today.length-s.completedToday)}</div><div class="metric-foot"><span>${s.overdue.length} overdue overall</span><span>Today</span></div></div><div class="card metric"><div class="metric-top"><span>Active priorities</span><span class="metric-icon"><span class="material-symbols-outlined">tune</span></span></div><div class="metric-value" style="font-size:20px">${s.priorities.map(p=>`${p.priority[0].toUpperCase()}:${p.count}`).join(' · ')}</div><div class="metric-foot"><span>High</span><span>Medium</span><span>Low</span></div></div></section>${quickAdd()}<section class="dashboard-columns"><div><div class="focus-toolbar"><h2 class="section-title">Today's Focus Tasks <small>${activeToday.length} active / ${today.length||0} scheduled</small></h2><div class="toolbar-right"><button class="icon-btn" data-action="sort-toggle" aria-label="Sort tasks"><span class="material-symbols-outlined">swap_vert</span></button><button class="icon-btn" data-action="new-task" aria-label="New task"><span class="material-symbols-outlined">add_task</span></button></div></div><div class="task-list">${focus.length?focus.map(t=>taskCard(t)).join(''):emptyState('Nothing scheduled for today','Add a task with a due date of today to build your focus queue.','event')}</div>${recentDone.length?`<div style="margin-top:18px"><div class="group-head"><span class="group-title">Completed today <span class="badge category">${recentDone.length}</span></span><button class="icon-btn" data-route="completed" aria-label="View completed"><span class="material-symbols-outlined">arrow_forward</span></button></div><div class="task-list" style="margin-top:6px">${recentDone.map(t=>taskCard(t)).join('')}</div></div>`:''}</div><div class="side-stack"><section class="card side-card"><h3>Weekly Velocity <span class="success mono" style="font-size:9px">${s.total?Math.round(s.completed/Math.max(1,s.total)*100):0}% complete</span></h3><p>Throughput across the current task set.</p><div class="velocity-chart"><div class="bar" style="height:32%"></div><div class="bar" style="height:54%"></div><div class="bar current" style="height:78%"></div><div class="bar" style="height:42%"></div><div class="bar" style="height:28%"></div></div><div class="chart-labels"><span>Mon</span><span>Tue</span><span>Wed</span><span>Thu</span><span>Fri</span></div></section><section class="card side-card"><div style="display:flex;justify-content:space-between;gap:8px"><h3>Upcoming Horizon</h3><button class="btn secondary" style="min-height:28px;padding:0 9px;font-size:9px" data-route="upcoming">View all</button></div>${s.upcoming.slice(0,3).map(t=>`<div class="horizon-item"><strong>${escapeHtml(t.title)}</strong><span>${escapeHtml(relativeDue(t.dueDate))} · ${escapeHtml(t.category)}</span></div>`).join('')||'<div class="muted" style="font-size:11px;margin-top:8px">No upcoming tasks.</div>'}<div class="focus-suggestion"><span class="material-symbols-outlined" style="font-size:15px;vertical-align:-4px">auto_awesome</span> Focus suggestion: ${s.overdue.length?'Clear an overdue item before adding more queue depth.':'Finish the highest-priority active task first for the cleanest next milestone.'}</div></section></div></section></div>`;
}

export function renderAllTasks(tasks,state){
  const filtered=filterTasks(tasks,{...state.filters}); const categories=['all','Engineering','Design','Product','Personal','Operations']; const activeCount=tasks.filter(t=>!t.completed).length; const overdue=tasks.filter(t=>!t.completed&&t.dueDate&&daysFromToday(t.dueDate)<0); const week=tasks.filter(t=>!t.completed&&daysFromToday(t.dueDate)>=0&&daysFromToday(t.dueDate)<=7);
  return `<div class="page"><section class="glass-card" style="padding:20px"><div class="page-head"><div class="page-head-copy"><div class="eyebrow"><span class="dot" style="color:var(--success)"></span>Task control surface</div><h1>All Tasks <span class="badge category" style="vertical-align:middle">${activeCount} active</span>${overdue.length?` <span class="badge high" style="vertical-align:middle">${overdue.length} overdue</span>`:''}</h1><p>Manage, organize, and track your active initiatives across engineering, design, product, and personal work.</p></div><div class="head-actions"><button class="btn primary" data-action="new-task"><span class="material-symbols-outlined">add_task</span>New Task</button></div></div><div class="stats-banner" style="margin-top:16px"><div class="stat-box"><div class="stat-number">${tasks.length?Math.round((tasks.filter(t=>t.completed).length/tasks.length)*100):0}%</div><div class="stat-label">Completion</div></div><div class="stat-box"><div class="stat-number">${week.length}</div><div class="stat-label">Due this week</div></div><div class="stat-box"><div class="stat-number">${tasks.filter(t=>!t.completed&&!t.dueDate).length}</div><div class="stat-label">Backlog</div></div><div class="stat-box"><div class="stat-number" style="color:#ffb4ab">${overdue.length}</div><div class="stat-label">Needs attention</div></div></div></section><section class="toolbar-card"><div class="toolbar-row"><div class="input-wrap"><span class="material-symbols-outlined search-icon">search</span><input id="all-search" class="input" style="padding-left:36px" value="${escapeHtml(state.filters.query||'')}" placeholder="Filter tasks by name, assignee, or tag..." aria-label="Filter tasks"></div><div class="toolbar-right"><select id="all-priority" class="select" style="width:150px"><option value="all">All priorities</option><option value="high" ${state.filters.priority==='high'?'selected':''}>High priority</option><option value="medium" ${state.filters.priority==='medium'?'selected':''}>Medium priority</option><option value="low" ${state.filters.priority==='low'?'selected':''}>Low priority</option></select><select id="all-sort" class="select" style="width:160px"><option value="due-asc" ${state.filters.sort==='due-asc'?'selected':''}>Sort: Due date</option><option value="due-desc" ${state.filters.sort==='due-desc'?'selected':''}>Sort: Due date desc</option><option value="priority" ${state.filters.sort==='priority'?'selected':''}>Sort: Priority</option><option value="alphabetical" ${state.filters.sort==='alphabetical'?'selected':''}>Sort: Alphabetical</option><option value="created-oldest" ${state.filters.sort==='created-oldest'?'selected':''}>Sort: Oldest</option></select></div></div><div class="chip-row">${['all','active','completed'].map(c=>`<button class="chip ${state.filters.status===c?'active':''}" data-filter-status="${c}">${c} ${c==='all'?`(${tasks.length})`:c==='active'?`(${activeCount})`:`(${tasks.filter(t=>t.completed).length})`}</button>`).join('')} ${categories.map(c=>c==='all'?`<button class="chip ${state.filters.category==='all'?'active':''}" data-filter-category="all">All categories</button>`:`<button class="chip ${state.filters.category===c?'active':''}" data-filter-category="${c}">${c}</button>`).join('')} <button class="chip" data-action="reset-filters">Reset</button></div></section><section class="full-row">${filtered.length?`<div class="task-group"><div class="group-head"><span class="group-title"><span class="group-dot danger"></span>${state.filters.status==='completed'?'Completed':'Tasks'} <span class="badge category">${filtered.length}</span></span><span class="subtle mono">${state.filters.sort||'due-asc'}</span></div><div class="task-list">${filtered.map(t=>taskCard(t)).join('')}</div></div>`:emptyState('No tasks match your criteria','Clear a filter or search query, or create a new task.','search_off')}</section></div>`;
}

export function renderToday(tasks){
  const todayKey=new Date().toISOString().slice(0,10); const today=tasks.filter(t=>t.dueDate===todayKey); const completed=today.filter(t=>t.completed).length; const pct=today.length?Math.round(completed/today.length*100):0; const groups=[['Morning / Urgent',today.filter(t=>!t.dueTime||parseInt(t.dueTime)<12)],['Afternoon / Scheduled',today.filter(t=>t.dueTime&&parseInt(t.dueTime)>=12&&parseInt(t.dueTime)<17)],['Evening / Wrap-up',today.filter(t=>t.dueTime&&parseInt(t.dueTime)>=17)]];
  return `<div class="page"><section class="glass-card dashboard-hero"><div class="eyebrow"><span class="dot"></span>Live daily sprint <span class="mono muted">${formatShortDate()}</span> · <span class="success">On track</span></div><div class="page-head" style="margin-top:5px"><div class="page-head-copy"><h1>Today’s Focus</h1><p><span style="color:var(--warning);font-weight:700">!</span> ${today.length} tasks scheduled for today — ${today.filter(t=>t.priority==='high'&&!t.completed).length} high priority requiring active flow state.</p></div><div class="card" style="padding:10px 12px;background:rgba(6,14,32,.24);box-shadow:none"><div class="mono" style="font-size:11px">${pct}%</div><div class="muted" style="font-size:9px">Sprint velocity</div></div></div><div style="margin-top:18px"><div class="trajectory-bar"><span style="width:${pct}%"></span></div><div class="trajectory-scale"><span>Milestone</span><span>Current (${pct}%)</span><span>${Math.max(0,today.length-completed)} remaining</span></div></div></section>${quickAdd()}<section class="dashboard-columns"><div class="full-row">${groups.map(([label,items],i)=>`<section class="task-group"><div class="group-head"><span class="group-title"><span class="group-dot ${i===0?'danger':i===1?'':'subtle'}"></span>${label}<span class="badge category">${items.length} Tasks</span></span><span class="mono subtle">${i===0?'Due before 12:00 PM':i===1?'Due before 5:00 PM':'Quick wins & shutdown'}</span></div><div class="task-list">${items.length?items.map(t=>taskCard(t)).join(''):emptyState('No tasks in this time block','Use the quick capture bar to add one.','event')}</div></section>`).join('')}</div><div class="side-stack"><section class="card side-card"><h3>Today’s Time Blocking <span class="mono muted" style="font-size:9px">${formatShortDate()}</span></h3><p>Focused sprint slots vs. scheduled collaboration.</p>${[['09:00','Deep work'],['10:30','Design audit'],['12:00','Lunch & disconnect'],['14:00','Engineering sync'],['16:15','RFP defense'],['17:30','Inbox zero']].map((x,i)=>`<div style="display:flex;gap:9px;padding:9px 0;border-bottom:1px solid var(--border)"><span class="mono subtle" style="width:40px;font-size:9px">${x[0]}</span><div style="flex:1"><strong style="font-size:10px">${x[1]}</strong><div class="muted" style="font-size:9px">${i===1?'Active focus slot · 90m':i===3?'Calendar event · 45m':'Scheduled block'}</div></div></div>`).join('')}<div style="display:flex;justify-content:space-between;margin-top:9px;color:var(--muted);font-size:10px"><span>Deep work buffer</span><span class="success mono">3.5 hrs</span></div></section><section class="card side-card"><h3>Alex’s Productivity Index</h3><p>${today.filter(t=>t.priority==='high'&&t.completed).length} high-priority tasks finished today. Keep the queue tight and intentional.</p></section></div></section></div>`;
}

export function renderUpcoming(tasks){
  const today=new Date().toISOString().slice(0,10); const upcoming=tasks.filter(t=>!t.completed&&t.dueDate&&t.dueDate>today).sort((a,b)=>a.dueDate.localeCompare(b.dueDate)); const groups=[]; const seen=new Map(); upcoming.forEach(t=>{const k=t.dueDate; if(!seen.has(k))seen.set(k,[]);seen.get(k).push(t);}); [...seen.entries()].slice(0,4).forEach(([date,items])=>groups.push({date,items})); const later=upcoming.slice(0,3);
  return `<div class="page"><section class="page-head"><div class="page-head-copy"><div class="eyebrow"><span class="dot"></span>Forward scheduling engine</div><h1>Upcoming Tasks</h1><p>Plan ahead and stay on top of scheduled commitments across engineering, design, and product initiatives.</p></div><div class="head-actions"><button class="btn secondary"><span class="material-symbols-outlined">view_timeline</span>Timeline</button><button class="btn secondary"><span class="material-symbols-outlined">calendar_month</span>Calendar</button><button class="btn secondary"><span class="material-symbols-outlined">layers</span>Group: Week</button><button class="btn primary" data-action="new-task"><span class="material-symbols-outlined">add_circle</span>Schedule Task</button></div></section><section class="glass-card completion-banner"><div class="completion-left"><div class="metric-icon" style="width:42px;height:42px"><span class="material-symbols-outlined">insights</span></div><div><strong style="font-size:13px">${upcoming.length} tasks scheduled ahead</strong><div class="muted" style="font-size:10px">Next milestone ${upcoming[0]?`is ${formatDate(upcoming[0].dueDate)} · ${upcoming[0].category}`:'will appear here once you schedule work'}.</div></div></div><span class="badge medium">Velocity alert</span></section><section class="timeline">${groups.length?groups.map((g,i)=>`<section class="timeline-group"><div class="timeline-marker"><span class="material-symbols-outlined">${i===0?'event_upcoming':i===1?'calendar_month':'flag'}</span></div><h2>${formatDate(g.date,{weekday:'long',month:'short',day:'numeric'})} <span class="badge category">${g.items.length} Tasks</span> <span class="mono subtle" style="font-size:9px">${relativeDue(g.date)}</span></h2><div class="timeline-items">${g.items.map(t=>`<article class="upcoming-item" data-task-id="${t.id}"><div class="date-block">${formatDate(t.dueDate,{month:'short',day:'2-digit'})}</div><div class="upcoming-main"><div class="task-meta" style="margin:0 0 3px">${priorityBadge(t.priority)}<span class="badge category">${escapeHtml(t.category)}</span><span class="mono subtle">${t.dueTime||''}</span></div><strong>${escapeHtml(t.title)}</strong><span>${escapeHtml(t.description||'No description')}</span></div><button class="upcoming-check" data-action="toggle-task" data-id="${t.id}" aria-label="Complete task"><span class="material-symbols-outlined">check</span></button></article>`).join('')}</div></section>`).join(''):emptyState('Your upcoming horizon is clear','Schedule a task with a future due date to see it on this timeline.','calendar_month')}</section>${upcoming.length?`<section class="later-grid">${later.map(t=>`<div class="later-card"><div><div class="task-meta" style="margin:0 0 5px">${priorityBadge(t.priority)}</div><strong>${escapeHtml(t.title)}</strong><p>${escapeHtml(t.description||'')}</p></div><div class="mono subtle" style="font-size:9px">${formatDate(t.dueDate)} · ${escapeHtml(t.category)}</div></div>`).join('')}</section>`:''}<div class="empty"><span class="material-symbols-outlined" style="color:var(--success)">celebration</span><h3>${upcoming.length?'Keep the horizon intentional.':'All caught up!'}</h3><p>${upcoming.length?'No scheduling conflicts are tracked by this local workspace.':'No future-dated active tasks are currently scheduled.'}</p></div></div>`;
}

export function renderCompleted(tasks,state){ const completed=filterTasks(tasks,{...state.filters,status:'completed',query:state.filters.query}); const total=tasks.filter(t=>t.completed).length; const pct=tasks.length?Math.round(total/tasks.length*100):0; return `<div class="page"><section class="page-head"><div class="page-head-copy"><div class="eyebrow"><span class="dot"></span>Audited archive</div><h1>Completed Tasks <span class="badge category" style="vertical-align:middle">${total} tasks finished</span></h1><p>Review resolved deliverables, restore completed work, and keep a clean completion history.</p></div><div class="head-actions"><button class="btn secondary" data-action="export"><span class="material-symbols-outlined">download</span>Export Archive</button><button class="btn danger" data-action="clear-history"><span class="material-symbols-outlined">delete_sweep</span>Clear History</button></div></section><section class="glass-card completion-banner"><div class="completion-left"><div class="completion-ring" style="--pct:${pct}%"><span>${pct}%</span></div><div><strong style="font-size:14px">Productivity Summary</strong><div class="muted" style="font-size:10px">${total} tasks fulfilled across the current workspace.</div></div></div><div class="badge ${pct>=70?'success':'medium'}">${pct>=70?'Steady throughput':'Building momentum'}</div></section><section class="toolbar-card"><div class="toolbar-row"><div class="input-wrap"><span class="material-symbols-outlined search-icon">search</span><input id="completed-search" class="input" style="padding-left:36px" value="${escapeHtml(state.filters.query||'')}" placeholder="Search completed tasks by keyword, category, ref..." aria-label="Search completed tasks"></div><div class="toolbar-right"><button class="btn secondary" data-action="reset-completed">Reset</button></div></div><div class="chip-row">${['all','Engineering','Design','Product','Operations'].map(c=>`<button class="chip ${state.filters.category===(c==='all'?'all':c)?'active':''}" data-filter-completed-category="${c}">${c} ${c==='all'?`(${total})`:''}</button>`).join('')}</div></section><section class="completed-list">${completed.length?completed.map(t=>`<article class="completed-card" data-task-id="${t.id}"><input type="checkbox" aria-label="Select task" data-select-task data-id="${t.id}" style="accent-color:var(--primary-strong)"><div class="check"><span class="material-symbols-outlined" style="font-size:15px">check</span></div><div><h3>${escapeHtml(t.title)}</h3><p>${escapeHtml(t.description||'Completed task')} · ${t.completedAt?new Date(t.completedAt).toLocaleString():'Completed'}</p></div><div class="task-actions"><button class="icon-btn" data-action="view-task" data-id="${t.id}" aria-label="View task"><span class="material-symbols-outlined">visibility</span></button><button class="icon-btn" data-action="toggle-task" data-id="${t.id}" aria-label="Restore task"><span class="material-symbols-outlined">undo</span></button><button class="icon-btn" data-action="delete-task" data-id="${t.id}" aria-label="Delete task"><span class="material-symbols-outlined">delete</span></button></div></article>`).join(''):emptyState('No completed tasks','Finish an active task and it will appear in this archive.','check_circle')}</section></div>`; }

export function renderSettings(settings){
 return `<div class="page"><div class="page-head"><div class="page-head-copy"><div class="eyebrow"><span class="dot"></span>System / configuration</div><h1>Settings & Preferences</h1><p>Manage ambient workspace parameters, notifications, and task behavior without leaving the single-page workspace.</p></div><div class="badge success"><span class="dot" style="width:5px;height:5px;box-shadow:none"></span>Cloud sync: local persistence</div></div><div class="settings-layout"><nav class="card settings-nav">${['General','Appearance & Theme','Notifications','Account & Security','Integrations'].map((x,i)=>`<button class="${i===1?'active':''}" type="button">${x}</button>`).join('')}</nav><div class="settings-stack"><section class="card settings-card"><div class="settings-head"><div><h2><span class="material-symbols-outlined" style="font-size:17px;vertical-align:-4px">palette</span> Appearance & Theme</h2><p>Calibrate interface exposure and the visual system.</p></div><span class="badge category">Kinetic clarity</span></div><div class="theme-cards"><button class="theme-option ${!document.documentElement.classList.contains('light')?'active':''}" data-theme-choice="dark"><div class="preview dark"><div class="top"></div><div class="body"></div></div><div class="option-title">Dark Mode</div><div class="option-sub">Nocturnal low-fatigue slate</div></button><button class="theme-option ${document.documentElement.classList.contains('light')?'active':''}" data-theme-choice="light"><div class="preview"><div class="top"></div><div class="body"></div></div><div class="option-title">Light Mode</div><div class="option-sub">High-clarity diurnal workspace</div></button><button class="theme-option" data-theme-choice="system"><div class="preview system"><div class="top"></div><div class="body"></div></div><div class="option-title">System Sync</div><div class="option-sub">Follows the browser preference</div></button></div><div class="toggle-row"><div class="toggle-copy"><strong>Compact task density</strong><span>Compress row spacing for higher information throughput.</span></div><button class="switch ${settings.compact?'on':''}" data-toggle-setting="compact" aria-pressed="${settings.compact}"><span></span></button></div></section><section class="card settings-card"><div class="settings-head"><div><h2><span class="material-symbols-outlined" style="font-size:17px;vertical-align:-4px;color:var(--success)">notifications</span> Notification Preferences</h2><p>Local preferences for reminders and completion feedback.</p></div><button class="btn secondary" data-action="test-alert">Test alert</button></div><div class="toggle-row"><div class="toggle-copy"><strong>Due date reminders</strong><span>Use the visual priority states to keep upcoming work visible.</span></div><button class="switch on" aria-pressed="true"><span></span></button></div><div class="toggle-row"><div class="toggle-copy"><strong>Overdue task alerts</strong><span>Highlight past-due active tasks in task lists and dashboard metrics.</span></div><button class="switch on" aria-pressed="true"><span></span></button></div><div class="toggle-row"><div class="toggle-copy"><strong>Sound effects on task completion</strong><span>Simple interface preference stored locally in this browser.</span></div><button class="switch ${settings.sound?'on':''}" data-toggle-setting="sound" aria-pressed="${settings.sound}"><span></span></button></div></section><section class="card settings-card"><div class="settings-head"><div><h2><span class="material-symbols-outlined" style="font-size:17px;vertical-align:-4px">person</span> Account & Security</h2><p>Workspace identity fields are local UI data in this build.</p></div><span class="badge success">Vault secure</span></div><div class="two-col" style="margin-top:10px"><div class="toggle-row" style="margin-top:0"><div class="toggle-copy"><strong>Alex Morgan</strong><span>Workspace owner</span></div><span class="avatar">AM</span></div><div class="toggle-row" style="margin-top:0"><div class="toggle-copy"><strong>alex@taskflow.io</strong><span>Primary workspace email</span></div><span class="material-symbols-outlined" style="color:var(--success)">verified</span></div></div><div class="two-col" style="margin-top:8px"><button class="btn secondary">Change Password</button><button class="btn secondary">Regenerate Recovery Keys</button></div></section><section class="card settings-card"><div class="settings-head"><div><h2><span class="material-symbols-outlined" style="font-size:17px;vertical-align:-4px;color:var(--warning)">hub</span> Connected Integrations</h2><p>Connection placeholders retained from the supplied design; this local build does not send external data.</p></div><span class="badge category">3 slots</span></div><div class="two-col" style="margin-top:9px"><div class="toggle-row" style="margin-top:0"><div class="toggle-copy"><strong>Slack</strong><span>Daily sprint updates</span></div><span class="success mono" style="font-size:10px">●</span></div><div class="toggle-row" style="margin-top:0"><div class="toggle-copy"><strong>GitHub</strong><span>Linked task references</span></div><span class="success mono" style="font-size:10px">●</span></div></div></section><section class="card settings-card danger-zone"><div class="settings-head"><div><h2><span class="material-symbols-outlined" style="font-size:17px;vertical-align:-4px">warning</span> Danger Zone</h2><p>Destructive operations permanently modify local workspace data.</p></div></div><div class="toggle-row" style="margin-top:10px"><div class="toggle-copy"><strong>Archive completed tasks</strong><span>Export the completed archive before clearing history.</span></div><button class="btn secondary" data-action="export"><span class="material-symbols-outlined">archive</span>Archive tasks</button></div><div class="toggle-row"><div class="toggle-copy"><strong style="color:#ffb4ab">Delete all tasks</strong><span>Requires an explicit confirmation before local storage is cleared.</span></div><button class="btn danger" data-action="delete-workspace"><span class="material-symbols-outlined">delete_forever</span>Delete workspace</button></div></section></div></div></div>`;
}
